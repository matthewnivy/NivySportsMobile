import {StyleSheet} from 'react-native';

import {fontFamily} from '../../comman';

export const stylesInput = StyleSheet.create({
  input: {
    height: 40,
    borderColor: '#C6C6C6',
    borderWidth: 1,
    backgroundColor: '#F9F9F9',
    fontFamily: fontFamily.regular,
  },
});
